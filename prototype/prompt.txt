Evaluate the following resume against the specified target job role.

Target Role: {{ROLE}}

Resume Content:
{{RESUME_TEXT}}

Return the evaluation strictly in valid JSON format with the following structure:

{
  "role": "{{ROLE}}",
  "readiness_score": number,
  "confidence_level": "High | Medium | Low",
  "strengths": [string],
  "skill_gaps": [string],
  "next_steps": [string],
  "evidence": {
    "skill_name": "resume evidence supporting the skill"
  }
}

Rules:
- Evidence must directly reference resume content
- Do not infer skills without evidence
- Be objective and role-specific
- Output JSON only (no explanations)
